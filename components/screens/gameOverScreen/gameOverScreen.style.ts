import { colors } from '@/styles';
import { StyleSheet } from 'react-native';

const S = StyleSheet.create({
  screenWrapper: {},
  text: {
    fontSize: 50,
    fontWeight: 900,
    color: 'pink',
  },
});

export { S };
