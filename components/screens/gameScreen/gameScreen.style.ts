import { StyleSheet } from 'react-native';

const S = StyleSheet.create({
  screen: {
    flex: 1,
    paddingVertical: 30,
    paddingHorizontal: 20,
    borderWidth: 2,
    borderColor: 'red',
  },
});

export { S };
